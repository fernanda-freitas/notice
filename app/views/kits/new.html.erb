<%= form_for(@kit) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :description %>
  <%= f.text_field :description %>
  <%= f.submit 'Submit' %>
<% end %>

<!-- TODO : to add image upload if we would like.. >
